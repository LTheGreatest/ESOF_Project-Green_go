class UserModel {
  String username;
  String uid;
  int points = 0;

  String password;
  String email;

  // Constructor
  UserModel(this.uid, this.username, this.points, this.password, this.email);
}
